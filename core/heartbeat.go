package core

// Heartbeat import launch heartbeat checker of all connected nodes.

type Heartbeat struct {
}

func (m Metrics) NewHeartbeat() (err error) {

	//	check api and shuttle heartbeat and report to TiBD every 5 minutes.
	return
}
